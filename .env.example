# .env.example
# 請將本檔複製為 .env 後填寫；所有變數皆為字串。不得提交任何真實密鑰至版本控制。

# --- 應用程式核心設定 ---
NODE_ENV=
# 環境：development | production | test

PORT=
# 服務監聽埠（數字）。範例：3000（註：此行不填真值，僅說明）

HOST=
# 綁定主機。範例：0.0.0.0

BASE_URL=
# 對外完整位址（含協定與主機，結尾不含斜線）。範例：https://api.example.com

# --- Public（瀏覽器可讀）---
NEXT_PUBLIC_API_BASE_URL=
# 前端呼叫 API 的基底路徑；可用相對路徑 /api 或完整網址。

# --- 資料庫設定（擇一：DATABASE_URL 或離散參數）---
DATABASE_URL=
# 連線字串；格式：postgres://USER:PASSWORD@HOST:PORT/DB?sslmode=...

POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
# 若未提供 DATABASE_URL，將使用上述離散參數組合連線。

PGSSLMODE=
# SSL 模式：require | disable | prefer

# --- 身分驗證與會話 ---
JWT_SECRET=
# JWT 簽章密鑰（必填，高熵字串）。

SESSION_SECRET=
# 伺服端會話加密用密鑰（必填）。

ACCESS_TOKEN_TTL=
# Access Token 存活時間；格式：15m、1h、24h。

REFRESH_TOKEN_TTL=
# Refresh Token 存活時間；格式：7d、30d。

# --- 安全與跨域 ---
CORS_ORIGIN=
# 允許的來源（以逗號分隔）。開發環境可用 *（不建議用於生產）。

# --- 紀錄與偵錯 ---
LOG_LEVEL=
# 日誌層級：fatal | error | warn | info | debug | trace

LOG_PRETTY=
# 是否美化輸出：true | false

# --- 時區（選填）---
TZ=
# 服務運行時區；例如：UTC、Asia/Taipei。

# --- Next.js 選項（選填）---
NEXT_TELEMETRY_DISABLED=
# 關閉 Next.js 遙測：true | false
